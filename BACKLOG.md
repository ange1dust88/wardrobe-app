# Backlog

Запланированные правки и фичи (по приоритету не отсортировано).

## 1. Сообщение о несочетаемых вещах
Когда в аутфит выбраны несочетаемые айтемы (конфликт категорий — напр. dress + top, bottom + skirt; или противоположные сезоны summer↔winter), показывать табличку/нотис с объяснением, почему они не сочетаются.
- Бэк уже умеет определять конфликты: `categoriesConflict` и `seasonsConflict` (wardrobe-api/src/matching).
- Нужно: вывести это на фронт (панель в сборщике или у колеса) — какие именно пары конфликтуют.

## 2. Объяснение скора при наведении
При наведении на скор матча показывать разбор — что хорошо, что плохо (6 осей: color / role / season / palette / vibe / pattern).
- Бэк уже возвращает `breakdown` в `computeTotalScore` — нужно прокинуть его на фронт и показать в тултипе/поповере.

## 3. Доработка вида List (карусель)
Доточить outfit carousel (внешний вид и поведение лент).

## 4. UX / подтверждения / загрузки
- Подтверждения на необратимые действия (удаление вещи/аутфита).
- Состояния загрузки и ошибок, скелетоны.
- Общая полировка user-flow.

## 5. Папки в аутфитах
Группировка сохранённых луков по папкам/коллекциям.
- Нужно: модель (folder) на бэке + UI в виде Outfits.
